# D2 RTL plan

The software model, INT8 simulation, parameter export, and vector generator are implemented. The
full-network Verilog top and C++ testbench are not yet implemented. D2 can reuse the D1 operator set,
but all source, generated parameters, test vectors, and build products must remain inside this folder.


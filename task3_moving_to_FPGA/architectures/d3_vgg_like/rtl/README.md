# D3 RTL plan

The software model, INT8 simulation, parameter export, and vector generator are implemented. The
full-network Verilog top and C++ testbench are not yet implemented. This design adds two convolution
stages but does not require a new arithmetic operator beyond the D1 operator set.

